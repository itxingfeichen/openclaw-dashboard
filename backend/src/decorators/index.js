/**
 * 装饰器模块导出
 */

export {
  Cacheable,
  CacheableConfig,
  CacheInvalidate,
  ConditionalCache,
  createCacheable,
  createCacheInvalidate,
  cacheable,
  cacheInvalidate,
} from './cacheable.js'

export default {
  Cacheable,
  CacheableConfig,
  CacheInvalidate,
  ConditionalCache,
  createCacheable,
  createCacheInvalidate,
  cacheable,
  cacheInvalidate,
}
